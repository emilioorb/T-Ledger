# Landing de T-Ledger — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Una landing pública de una sola pantalla, con forma de cuenta T, en `/`, con el tablero mudado a `/tablero`.

**Architecture:** Una ruta pública sin shell (como `/entrar`), compuesta por cuatro archivos nuevos en `features/landing`: los datos del asiento —que son datos y tienen su prueba—, la composición de la cuenta T, el modal de acceso y la pantalla que los junta. El movimiento de rutas va en su propia tarea, al final, porque es lo que deja la aplicación andando con la landing puesta.

**Tech Stack:** React 19, TanStack Router (rutas por archivo), Tailwind v4, shadcn/ui (`Dialog`, `Button`), Vitest + Testing Library.

**Spec:** `docs/plans/2026-09-22-landing-design.md`

## Global Constraints

- **Español de Costa Rica, voseo.** Todo el texto visible vive en un `copy.ts`, nunca suelto en el JSX.
- **TypeScript siempre; `any` nunca.** Tipos propios, genéricos o `unknown`.
- **Comentarios solo donde la lógica no se explica sola**, y explican el *porqué*, no el *qué*. Es el estándar del repositorio: mirá cualquier archivo de `features/` antes de escribir el primero.
- **Monocromo.** Ni un color nuevo. La jerarquía se hace con luminancia, peso y espacio (DESIGN.md, Regla del Monocromo).
- **Cero dependencias nuevas.** Nada de React Bits, nada de Three.js, nada de librerías de animación.
- **Prettier:** `--no-semi --single-quote --print-width 100`. Nunca sobre `web/src/components/ui/**`.
- **Nombre del producto:** `shell.app.name` (`T-Ledger`). No se escribe a mano en ningún lado.
- **Correo de contacto:** `emiliorb@arclosystems.com`, el mismo que ya usan `identity.marco.correoDeAyuda` y `shell.nav.supportEmail`.
- **Cifras:** siempre con `formatMoney` de `@/lib/money` y con la clase `num` (mono tabular).
- **Accesibilidad:** WCAG AA en los dos temas, foco visible, `prefers-reduced-motion` respetado.
- Todos los comandos se corren desde `web/`.

## File Structure

| Archivo | Responsabilidad |
|---|---|
| `web/src/features/landing/copy.ts` | Todo el texto de la landing y del modal. |
| `web/src/features/landing/asientos.ts` | Los tres asientos de ejemplo y la suma de cada lado. Puro, sin React. |
| `web/src/features/landing/asientos.spec.ts` | Que los dos lados sumen lo mismo. Es la prueba de la promesa del titular. |
| `web/src/features/landing/cuenta-t.tsx` | La composición: travesaño, pata, los tres asientos y los totales. |
| `web/src/features/landing/cuenta-t.spec.tsx` | Que se rendericen las glosas, las cifras y los dos totales. |
| `web/src/features/landing/pedir-acceso.tsx` | El botón y su modal con la dirección, copiar y escribir. |
| `web/src/features/landing/pedir-acceso.spec.tsx` | Que copie al portapapeles y que el `mailto:` lleve el asunto. |
| `web/src/features/landing/landing.tsx` | La pantalla: cabecera, titular, cuenta T, pie, y el salto a `/tablero` si hay sesión. |
| `web/src/routes/index.tsx` | Reescrito: la ruta `/` pasa a renderizar la landing. |
| `web/src/routes/tablero.tsx` | El tablero de hoy, movido tal cual desde `routes/index.tsx`. |
| `web/src/features/shell/page-title.spec.ts` | Nueva: fija el nombre de `/` y de `/tablero`. |

Modificados sin cambiar de responsabilidad: `routes/__root.tsx`, `features/shell/page-title.ts`, `features/shortcuts/destinations.ts`, `components/app-sidebar.tsx`, `components/not-found.tsx`, `components/nav-user.tsx`, `features/cuenta/crear-libro.tsx`, `features/cuenta/tus-libros.tsx`, `routes/entrar.tsx`, `routes/crear-cuenta.tsx`, `routes/guia.tsx`, `routes/contabilidad.patrimonio.tsx`.

---

### Task 1: Los asientos y su prueba

Los datos primero, porque son lo único de esta pantalla que puede estar mal de una manera que se pueda medir: si los dos lados no suman igual, el titular miente.

**Files:**
- Create: `web/src/features/landing/asientos.ts`
- Create: `web/src/features/landing/copy.ts`
- Test: `web/src/features/landing/asientos.spec.ts`

**Interfaces:**
- Consumes: `MoneyDto`, `formatMoney` de `@/lib/money`.
- Produces: `interface Asiento { glosa: string; debe: Renglon; haber: Renglon }`, `interface Renglon { cuenta: string; monto: MoneyDto }`, `const ASIENTOS: readonly Asiento[]`, `const totalDe: (asientos: readonly Asiento[], lado: 'debe' | 'haber') => MoneyDto`, y `copy` con las claves `titular`, `bajada`, `debe`, `haber`, `cierre`, `entrar`, `tab`, y el objeto `acceso`.

- [ ] **Step 1: Escribir la prueba que falla**

Crear `web/src/features/landing/asientos.spec.ts`:

```tsx
import { describe, expect, it } from 'vitest'
import { formatMoney } from '@/lib/money'
import { ASIENTOS, totalDe } from './asientos'

describe('los asientos de la landing', () => {
  it('cada uno cuadra por sí solo', () => {
    for (const asiento of ASIENTOS) {
      expect(asiento.debe.monto.minorUnits).toBe(asiento.haber.monto.minorUnits)
    }
  })

  it('los dos lados suman lo mismo, que es lo que la pantalla promete', () => {
    expect(totalDe(ASIENTOS, 'debe')).toEqual(totalDe(ASIENTOS, 'haber'))
  })

  it('el total es el que se lee al pie de la T', () => {
    // Con expresión regular y no con la cadena literal: `formatMoney` separa los miles con
    // U+202F, un espacio fino que no se distingue de uno normal al leerlo. Una prueba que
    // depende de un carácter invisible se rompe la primera vez que alguien la retipea.
    expect(formatMoney(totalDe(ASIENTOS, 'debe'))).toMatch(/^₡920\s000,00$/)
  })

  it('son tres: gastar, cobrar y abonar, que es lo que se hace todos los días', () => {
    expect(ASIENTOS).toHaveLength(3)
  })
})
```

- [ ] **Step 2: Correr la prueba y verla fallar**

Run: `npx vitest run src/features/landing/asientos.spec.ts`
Expected: FAIL, «Failed to resolve import "./asientos"».

- [ ] **Step 3: Escribir los datos**

Crear `web/src/features/landing/asientos.ts`:

```tsx
import type { CurrencyCode, MoneyDto } from '@/lib/money'

const MONEDA: CurrencyCode = 'CRC'

const colones = (minorUnits: string): MoneyDto => ({ minorUnits, currency: MONEDA })

export interface Renglon {
  cuenta: string
  monto: MoneyDto
}

export interface Asiento {
  // Lo que la persona escribiría con sus palabras. En contabilidad esto se llama glosa, y es
  // parte del asiento de verdad: sin ella, dentro de un mes nadie sabe qué fue ese movimiento.
  glosa: string
  debe: Renglon
  haber: Renglon
}

// Tres y no seis: son las tres cosas que el producto hace todos los días —gastar, cobrar y
// abonar— y tres pares es lo que entra en una pantalla sin apretar la tipografía.
//
// Las cuentas son las del plan que trae la app, no inventadas: quien entre después va a
// reconocer los mismos nombres.
export const ASIENTOS: readonly Asiento[] = [
  {
    glosa: 'Gasté 20 mil en el súper',
    debe: { cuenta: 'Mercado', monto: colones('2000000') },
    haber: { cuenta: 'Efectivo', monto: colones('2000000') },
  },
  {
    glosa: 'Me pagaron el salario',
    debe: { cuenta: 'Banco', monto: colones('85000000') },
    haber: { cuenta: 'Salario', monto: colones('85000000') },
  },
  {
    glosa: 'Abono 50 mil a la tarjeta',
    debe: { cuenta: 'Tarjeta', monto: colones('5000000') },
    haber: { cuenta: 'Banco', monto: colones('5000000') },
  },
]

// Con `BigInt` y no con `Number`: los montos vienen en céntimos como texto justamente porque
// un entero de JavaScript no los aguanta, y acá se suman igual que en el resto del producto.
export const totalDe = (asientos: readonly Asiento[], lado: 'debe' | 'haber'): MoneyDto =>
  colones(
    asientos.reduce((suma, asiento) => suma + BigInt(asiento[lado].monto.minorUnits), 0n).toString(),
  )
```

- [ ] **Step 4: Correr la prueba y verla pasar**

Run: `npx vitest run src/features/landing/asientos.spec.ts`
Expected: PASS, 4 pruebas.

- [ ] **Step 5: Escribir el copy**

Crear `web/src/features/landing/copy.ts`:

```tsx
// El texto de la única pantalla que ve alguien que todavía no entró. Vive acá por la misma
// razón que el resto: el titular se va a corregir diez veces, y buscarlo entre el JSX es
// buscarlo entre reglas y grillas.
export const copy = {
  // Lo que la pestaña dice mientras la landing está abierta. No lleva el nombre de la app
  // adelante: `documentTitleFor` ya lo agrega al final.
  tab: 'Contabilidad personal de partida doble',
  // No dice «contabilidad»: lo demuestra en vez de anunciarlo, y la palabra espanta a quien
  // no la usa. La bajada sí la dice, para quien la estaba buscando.
  titular: 'Vos anotás una cosa. El libro escribe dos.',
  bajada: 'Finanzas personales con contabilidad de partida doble de verdad.',
  debe: 'Debe',
  haber: 'Haber',
  // Sería marketing sin la segunda mitad. Con ella es la promesa que el producto cumple.
  cierre: 'Cuadra siempre, o te dice por qué no.',
  entrar: 'Entrar',
  acceso: {
    open: 'Solicitar acceso',
    title: 'Pedir acceso',
    description:
      'T-Ledger es de una persona y de quien esa persona invite. Si querés una cuenta, escribime.',
    email: 'emiliorb@arclosystems.com',
    subject: 'Quiero una cuenta en T-Ledger',
    copy: 'Copiar',
    copied: 'Copiada',
    // El portapapeles falla en contextos no seguros y cuando el navegador lo niega. Decirlo
    // es mejor que un botón que parpadea y no hace nada: la dirección está a la vista y se
    // puede seleccionar a mano.
    copyFailed: 'No se pudo copiar. La dirección está ahí arriba para seleccionarla.',
    write: 'Escribir',
  },
} as const
```

- [ ] **Step 6: Formatear y commitear**

```bash
npx prettier --no-semi --single-quote --print-width 100 --write src/features/landing
git add src/features/landing
git commit -m "✨ feat: los asientos de ejemplo de la landing, con su prueba de que cuadran"
```

---

### Task 2: La cuenta T

**Files:**
- Create: `web/src/features/landing/cuenta-t.tsx`
- Test: `web/src/features/landing/cuenta-t.spec.tsx`

**Interfaces:**
- Consumes: `ASIENTOS`, `totalDe` (Task 1), `copy` (Task 1), `formatMoney`, `cn` de `@/lib/utils`.
- Produces: `CuentaT`, componente sin props obligatorias: `({ className }: { className?: string })`.

**Nota sobre la composición, leela antes de escribir el JSX.** La columna izquierda es el **debe** y la derecha el **haber**, que es lo que dicen los rótulos. La glosa —la frase de la persona— va **cruzando las dos columnas**, arriba de su par, porque narra el asiento entero y no pertenece a ningún lado. Así el titular se ve cumplido literalmente: una frase arriba, dos anotaciones abajo, una de cada lado de la pata.

- [ ] **Step 1: Escribir la prueba que falla**

Crear `web/src/features/landing/cuenta-t.spec.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import { describe, expect, it } from 'vitest'
import { CuentaT } from './cuenta-t'

describe('CuentaT', () => {
  it('muestra la glosa de cada asiento', () => {
    render(<CuentaT />)
    expect(screen.getByText(/Gasté 20 mil en el súper/)).toBeInTheDocument()
    expect(screen.getByText(/Me pagaron el salario/)).toBeInTheDocument()
    expect(screen.getByText(/Abono 50 mil a la tarjeta/)).toBeInTheDocument()
  })

  it('pone cada cuenta de su lado', () => {
    render(<CuentaT />)
    expect(screen.getByText('Mercado')).toBeInTheDocument()
    expect(screen.getByText('Efectivo')).toBeInTheDocument()
    expect(screen.getByText('Salario')).toBeInTheDocument()
  })

  it('cierra con los dos totales, y son el mismo', () => {
    render(<CuentaT />)
    expect(screen.getAllByText(/920\s000,00/)).toHaveLength(2)
  })

  it('rotula las dos columnas', () => {
    render(<CuentaT />)
    expect(screen.getByText('Debe')).toBeInTheDocument()
    expect(screen.getByText('Haber')).toBeInTheDocument()
  })
})
```

- [ ] **Step 2: Correr la prueba y verla fallar**

Run: `npx vitest run src/features/landing/cuenta-t.spec.tsx`
Expected: FAIL, «Failed to resolve import "./cuenta-t"».

- [ ] **Step 3: Escribir el componente**

Crear `web/src/features/landing/cuenta-t.tsx`:

```tsx
import { formatMoney } from '@/lib/money'
import { cn } from '@/lib/utils'
import { ASIENTOS, totalDe, type Renglon } from './asientos'
import { copy } from './copy'

// Un renglón del asiento: la cuenta a la izquierda y la cifra a la derecha, que es como se
// escribe en un libro y como se lee en el resto del producto.
//
// Se llama `Linea` y no `Renglon` porque `Renglon` ya es el tipo que llega importado, y dos
// declaraciones con el mismo nombre en el mismo módulo no compilan.
const Linea = ({ cuenta, monto }: Renglon) => (
  <span className="grid grid-cols-[1fr_auto] items-baseline gap-x-4">
    <span className="truncate">{cuenta}</span>
    <span className="num tabular-nums">{formatMoney(monto)}</span>
  </span>
)

const Rotulo = ({ children, className }: { children: string; className?: string }) => (
  <span className={cn('text-xs tracking-wide text-muted-foreground uppercase', className)}>
    {children}
  </span>
)

// La cuenta T.
//
// La pata es un solo elemento absoluto y no una columna de la grilla: con una celda por fila,
// cualquier separación vertical la deja cortada a pedazos, y una T con la pata a rayas no es
// una T. Así corre entera desde los rótulos hasta la raya de totales.
//
// La glosa cruza las dos columnas porque narra el asiento entero. El debe va a la izquierda y
// el haber a la derecha: si se invierte, la página deja de ser verdad y quien lleva libros lo
// ve antes de leer el titular.
export const CuentaT = ({ className }: { className?: string }) => {
  const total = formatMoney(totalDe(ASIENTOS, 'debe'))

  return (
    <div className={cn('relative mx-auto w-full max-w-3xl', className)}>
      <div
        aria-hidden="true"
        className="absolute inset-y-0 left-1/2 hidden w-px bg-border md:block"
      />

      <div className="grid grid-cols-1 gap-x-0 md:grid-cols-2">
        <Rotulo className="hidden pr-8 text-right md:block">{copy.debe}</Rotulo>
        <Rotulo className="hidden pl-8 md:block">{copy.haber}</Rotulo>

        {ASIENTOS.map((asiento) => (
          <div key={asiento.glosa} className="contents">
            <p className="col-span-full pt-6 pb-1.5 text-center text-sm text-muted-foreground">
              «{asiento.glosa}»
            </p>

            {/* En teléfono las dos anotaciones se apilan con un filete a la izquierda: es la
                misma lectura girada noventa grados, porque dos columnas de cuarenta
                caracteres no entran en 320 píxeles. */}
            <p className="border-l border-border pl-3 text-sm md:border-0 md:pr-8 md:pl-0">
              <Linea {...asiento.debe} />
            </p>
            <p className="border-l border-border pl-3 text-sm md:border-0 md:pl-8">
              <Linea {...asiento.haber} />
            </p>
          </div>
        ))}

        {/* La doble raya del cierre: `border-double` es la que usa la contabilidad desde que
            los libros eran de papel, y acá es el remate de la pata. */}
        <p className="mt-6 border-t-[3px] border-double border-border pt-2 text-sm md:pr-8">
          <span className="num tabular-nums block text-right">{total}</span>
        </p>
        <p className="mt-6 border-t-[3px] border-double border-border pt-2 text-sm md:pl-8">
          <span className="num tabular-nums block text-right md:text-left">{total}</span>
        </p>
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Correr la prueba y verla pasar**

Run: `npx vitest run src/features/landing/cuenta-t.spec.tsx`
Expected: PASS, 4 pruebas.

- [ ] **Step 5: Commitear**

```bash
npx prettier --no-semi --single-quote --print-width 100 --write src/features/landing
git add src/features/landing
git commit -m "✨ feat: la cuenta T de la landing"
```

---

### Task 3: El modal de acceso

**Files:**
- Create: `web/src/features/landing/pedir-acceso.tsx`
- Test: `web/src/features/landing/pedir-acceso.spec.tsx`

**Interfaces:**
- Consumes: `copy.acceso` (Task 1), `Dialog`/`DialogContent`/`DialogHeader`/`DialogTitle`/`DialogDescription` de `@/components/ui/dialog`, `Button` de `@/components/ui/button`, `toast` de `sonner`.
- Produces: `PedirAcceso`, componente sin props. Renderiza su propio botón disparador.

- [ ] **Step 1: Escribir la prueba que falla**

Crear `web/src/features/landing/pedir-acceso.spec.tsx`:

```tsx
import { render, screen } from '@testing-library/react'
import userEvent from '@testing-library/user-event'
import { beforeEach, describe, expect, it, vi } from 'vitest'
import { copy } from './copy'
import { PedirAcceso } from './pedir-acceso'

const escribir = vi.fn<(texto: string) => Promise<void>>()

beforeEach(() => {
  escribir.mockReset()
  escribir.mockResolvedValue(undefined)
  Object.assign(navigator, { clipboard: { writeText: escribir } })
})

describe('PedirAcceso', () => {
  it('abre el modal con la dirección a la vista', async () => {
    render(<PedirAcceso />)
    await userEvent.click(screen.getByRole('button', { name: copy.acceso.open }))
    expect(screen.getByText(copy.acceso.email)).toBeInTheDocument()
  })

  it('copia la dirección al portapapeles', async () => {
    render(<PedirAcceso />)
    await userEvent.click(screen.getByRole('button', { name: copy.acceso.open }))
    await userEvent.click(screen.getByRole('button', { name: copy.acceso.copy }))
    expect(escribir).toHaveBeenCalledWith(copy.acceso.email)
  })

  it('el enlace de escribir lleva el asunto puesto', async () => {
    render(<PedirAcceso />)
    await userEvent.click(screen.getByRole('button', { name: copy.acceso.open }))
    const enlace = screen.getByRole('link', { name: copy.acceso.write })
    expect(enlace).toHaveAttribute('href', expect.stringContaining('mailto:'))
    expect(enlace.getAttribute('href')).toContain(encodeURIComponent(copy.acceso.subject))
  })
})
```

- [ ] **Step 2: Correr la prueba y verla fallar**

Run: `npx vitest run src/features/landing/pedir-acceso.spec.tsx`
Expected: FAIL, «Failed to resolve import "./pedir-acceso"».

- [ ] **Step 3: Escribir el componente**

Crear `web/src/features/landing/pedir-acceso.tsx`:

```tsx
import { useState } from 'react'
import { toast } from 'sonner'
import { Button } from '@/components/ui/button'
import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogHeader,
  DialogTitle,
} from '@/components/ui/dialog'
import { copy } from './copy'

const MAILTO = `mailto:${copy.acceso.email}?subject=${encodeURIComponent(copy.acceso.subject)}`

// Pedir acceso no es registrarse: no hay formulario, no hay endpoint y no se guarda nada. Del
// otro lado hay una persona, y la pantalla no finge que hay un sistema.
//
// El modal y no solo el `mailto:`, que es lo barato: quien no tiene cliente de correo
// configurado toca el enlace y no pasa nada, y no se entera de por qué. Con la dirección a la
// vista siempre hay salida.
export const PedirAcceso = () => {
  const [abierto, setAbierto] = useState(false)
  const [copiada, setCopiada] = useState(false)

  const copiar = async () => {
    try {
      await navigator.clipboard.writeText(copy.acceso.email)
      setCopiada(true)
    } catch {
      // El portapapeles no existe en contextos no seguros y el navegador puede negarlo. La
      // dirección sigue seleccionable a mano, así que esto se dice y no se disimula.
      toast.error(copy.acceso.copyFailed)
    }
  }

  return (
    <>
      <Button variant="ghost" size="sm" onClick={() => setAbierto(true)}>
        {copy.acceso.open}
      </Button>

      <Dialog
        open={abierto}
        onOpenChange={(valor) => {
          setAbierto(valor)
          if (!valor) setCopiada(false)
        }}
      >
        <DialogContent className="sm:max-w-md">
          <DialogHeader>
            <DialogTitle>{copy.acceso.title}</DialogTitle>
            <DialogDescription>{copy.acceso.description}</DialogDescription>
          </DialogHeader>

          {/* `select-all`: un clic la selecciona entera. Es la salida para cuando el botón de
              copiar no puede hacer su trabajo. */}
          <p className="num rounded-md border border-border bg-muted/40 px-3 py-2 text-center text-sm select-all">
            {copy.acceso.email}
          </p>

          <div className="flex justify-end gap-2">
            <Button variant="ghost" size="sm" onClick={() => void copiar()}>
              {copiada ? copy.acceso.copied : copy.acceso.copy}
            </Button>
            <Button size="sm" asChild>
              <a href={MAILTO}>{copy.acceso.write}</a>
            </Button>
          </div>
        </DialogContent>
      </Dialog>
    </>
  )
}
```

- [ ] **Step 4: Correr la prueba y verla pasar**

Run: `npx vitest run src/features/landing/pedir-acceso.spec.tsx`
Expected: PASS, 3 pruebas.

Si falla con «Cannot redefine property: clipboard», cambiá el `Object.assign` de la prueba por `Object.defineProperty(navigator, 'clipboard', { value: { writeText: escribir }, configurable: true })`.

- [ ] **Step 5: Commitear**

```bash
npx prettier --no-semi --single-quote --print-width 100 --write src/features/landing
git add src/features/landing
git commit -m "✨ feat: pedir acceso abre un modal con la dirección, sin guardar nada"
```

---

### Task 4: La pantalla

**Files:**
- Create: `web/src/features/landing/landing.tsx`

**Interfaces:**
- Consumes: `CuentaT` (Task 2), `PedirAcceso` (Task 3), `copy` (Task 1), `copy as shell` de `@/features/shell/copy`, `useSession` de `@/features/identity/auth-client`, `forceTheme` de `@/lib/theme`, `Link`/`useNavigate` de `@tanstack/react-router`, `Button`.
- Produces: `Landing`, componente sin props.

- [ ] **Step 1: Escribir la pantalla**

Crear `web/src/features/landing/landing.tsx`:

```tsx
import { useEffect, useLayoutEffect } from 'react'
import { Link, useNavigate } from '@tanstack/react-router'
import { ArrowRightIcon } from 'lucide-react'
import { Button } from '@/components/ui/button'
import { useSession } from '@/features/identity/auth-client'
import { copy as shell } from '@/features/shell/copy'
import { forceTheme } from '@/lib/theme'
import { CuentaT } from './cuenta-t'
import { copy } from './copy'
import { PedirAcceso } from './pedir-acceso'

// La única pantalla pública que no pide nada.
//
// Pinta primero y pregunta después: la guarda de la raíz no consulta la sesión en las rutas
// públicas, así que el visitante anónimo —que es para quien existe esta página— no espera una
// respuesta que en su caso siempre es «no». Si resulta que hay sesión, recién ahí salta al
// tablero, y con `replace` para no dejar la landing en el historial de quien ya entró.
export const Landing = () => {
  const navegar = useNavigate()
  const { data: sesion } = useSession()

  // Papel blanco con tinta negra, sea cual sea la preferencia guardada, igual que `/entrar` y
  // `/crear-cuenta`: las tres son la misma superficie, y el botón de entrar lleva de una a la
  // otra. Con la landing en oscuro, tocarlo encandila.
  //
  // `useLayoutEffect` y no `useEffect`, por lo mismo que en `MarcoDeIdentidad`: en un efecto
  // normal la pantalla asoma un cuadro con el tema viejo y da un parpadeo. `forceTheme`
  // devuelve la limpieza que restaura la preferencia al salir.
  useLayoutEffect(() => forceTheme('light'), [])

  useEffect(() => {
    if (sesion) void navegar({ to: '/tablero', replace: true })
  }, [navegar, sesion])

  return (
    <main className="flex min-h-dvh flex-col bg-background px-6 py-5 sm:px-10">
      <header className="flex items-center justify-between gap-4">
        {/* En monoespaciada, como en la pantalla de entrar: el nombre viene de la cuenta T, y
            dos columnas solo se leen derechas cuando alinean. */}
        <span className="font-mono text-lg font-medium tracking-tight">{shell.app.name}</span>

        <nav className="flex items-center gap-1 sm:gap-2">
          <PedirAcceso />
          <Button size="sm" asChild>
            <Link to="/entrar">
              {copy.entrar}
              <ArrowRightIcon aria-hidden="true" />
            </Link>
          </Button>
        </nav>
      </header>

      {/* El travesaño de la T, de borde a borde: es la única regla gruesa de la página, y lo
          que la hace leer como una cuenta T y no como una cabecera con línea abajo. */}
      <div
        aria-hidden="true"
        className="-mx-6 mt-5 border-t-2 border-foreground/70 sm:-mx-10"
      />

      <div className="flex flex-1 flex-col justify-center py-10">
        <h1 className="mx-auto max-w-[24ch] text-center text-3xl font-semibold tracking-tight text-balance sm:text-4xl">
          {copy.titular}
        </h1>
        <p className="mx-auto mt-3 max-w-[50ch] text-center text-sm text-muted-foreground">
          {copy.bajada}
        </p>

        <CuentaT className="mt-10 sm:mt-12" />

        <p className="mt-6 text-center text-sm text-muted-foreground">{copy.cierre}</p>
      </div>

      {/* El mismo pie que adentro y que en la pantalla de entrar: el símbolo, el año y el
          nombre juntos, y la versión después del separador, que es lo que cambia. */}
      <footer className="flex flex-wrap items-center justify-center gap-2 text-xs text-muted-foreground">
        <span>
          © <span className="num">{__BUILD_YEAR__}</span>{' '}
          <span className="text-foreground">{shell.app.name}</span>
        </span>
        <span aria-hidden="true">·</span>
        <span className="num">v{__APP_VERSION__}</span>
      </footer>
    </main>
  )
}
```

- [ ] **Step 2: Comprobar que compila**

Run: `npx tsc --noEmit -p tsconfig.app.json`
Expected: un único error, `Cannot find module '/tablero'` o equivalente en la ruta del `navigate`, porque `/tablero` todavía no existe. Cualquier otro error es tuyo y hay que arreglarlo ahora.

Si el error de `/tablero` rompe la compilación entera, dejá el `navigate` comentado con un `// TODO Task 5` **y descomentalo en la Task 5**; es la única excepción de este plan y no puede sobrevivir a la próxima tarea.

- [ ] **Step 3: Commitear**

```bash
npx prettier --no-semi --single-quote --print-width 100 --write src/features/landing
git add src/features/landing
git commit -m "✨ feat: la pantalla de la landing, con forma de cuenta T"
```

---

### Task 5: Mudar el tablero a `/tablero` y poner la landing en `/`

La tarea que deja la aplicación andando con la landing puesta. Es mecánica y larga: no inventes nada acá, solo movés referencias.

**Files:**
- Create: `web/src/routes/tablero.tsx` (movido desde `web/src/routes/index.tsx`)
- Modify: `web/src/routes/index.tsx` (reescrito entero)
- Modify: `web/src/routes/__root.tsx:136`
- Modify: `web/src/features/shell/page-title.ts:47-68`
- Modify: `web/src/features/shortcuts/destinations.ts:19`
- Modify: `web/src/components/app-sidebar.tsx:40`, `:99`
- Modify: `web/src/components/not-found.tsx:12`
- Modify: `web/src/components/nav-user.tsx:119`
- Modify: `web/src/features/cuenta/crear-libro.tsx:53`
- Modify: `web/src/features/cuenta/tus-libros.tsx:56`
- Modify: `web/src/routes/entrar.tsx:33`
- Modify: `web/src/routes/crear-cuenta.tsx:50`, `:64`
- Modify: `web/src/routes/guia.tsx:71`
- Modify: `web/src/routes/contabilidad.patrimonio.tsx:66`
- Test: `web/src/features/shell/page-title.spec.ts` (nuevo)

**Interfaces:**
- Consumes: `Landing` (Task 4), `copy` de `features/landing` (Task 1).
- Produces: la ruta `/` pública con la landing y la ruta `/tablero` con el tablero.

- [ ] **Step 1: Escribir la prueba que falla**

Crear `web/src/features/shell/page-title.spec.ts`:

```tsx
import { describe, expect, it } from 'vitest'
import { copy as landing } from '@/features/landing/copy'
import { copy as overview } from '@/features/projection/overview-copy'
import { documentTitleFor, screenNameFor } from './page-title'

describe('screenNameFor', () => {
  it('la raíz es la landing', () => {
    expect(screenNameFor('/')).toBe(landing.tab)
  })

  it('el tablero vive en /tablero', () => {
    expect(screenNameFor('/tablero')).toBe(overview.overview.title)
  })

  it('una ruta que no existe se dice que no existe', () => {
    expect(screenNameFor('/lo-que-sea')).not.toBe(overview.overview.title)
  })
})

describe('documentTitleFor', () => {
  it('pone el nombre de la app al final', () => {
    expect(documentTitleFor('/tablero')).toBe(`${overview.overview.title} · T-Ledger`)
  })
})
```

- [ ] **Step 2: Correr la prueba y verla fallar**

Run: `npx vitest run src/features/shell/page-title.spec.ts`
Expected: FAIL en «la raíz es la landing» (hoy devuelve `Dashboard`).

- [ ] **Step 3: Mover el tablero**

```bash
git mv src/routes/index.tsx src/routes/tablero.tsx
```

En `src/routes/tablero.tsx`, última línea, cambiar:

```tsx
export const Route = createFileRoute('/')({ component: DashboardScreen })
```

por:

```tsx
export const Route = createFileRoute('/tablero')({ component: DashboardScreen })
```

- [ ] **Step 4: Escribir la ruta de la landing**

Crear `src/routes/index.tsx` con esto y nada más:

```tsx
import { createFileRoute } from '@tanstack/react-router'
import { Landing } from '@/features/landing/landing'

export const Route = createFileRoute('/')({ component: Landing })
```

Si en la Task 4 dejaste el `navigate` comentado, descomentalo ahora.

- [ ] **Step 5: Abrir la raíz al público**

En `src/routes/__root.tsx:136`, cambiar:

```tsx
const PUBLICAS = ['/entrar', '/crear-cuenta']
```

por:

```tsx
// La landing entra acá por dos motivos a la vez: no pide sesión, y no lleva barra lateral.
// `beforeLoad` la compara contra el pathname y el componente contra el `routeId`, y para la
// raíz las dos cosas son '/'.
const PUBLICAS = ['/', '/entrar', '/crear-cuenta']
```

- [ ] **Step 6: Arreglar el nombre de las pantallas**

En `src/features/shell/page-title.ts`, agregar el import:

```tsx
import { copy as landing } from '@/features/landing/copy'
```

y dentro de `screenNameFor`, cambiar la primera línea del cuerpo:

```tsx
  if (!section) return overview.overview.title
```

por:

```tsx
  if (!section) return landing.tab
  if (section === 'tablero') return overview.overview.title
```

- [ ] **Step 7: Mover las referencias a `/`**

Una por una, cambiando **solo** el destino:

| Archivo | Línea | De | A |
|---|---|---|---|
| `src/features/shortcuts/destinations.ts` | 19 | `to: '/'` | `to: '/tablero'` |
| `src/components/app-sidebar.tsx` | 40 | `to: '/'` | `to: '/tablero'` |
| `src/components/app-sidebar.tsx` | 99 | `<Link to="/">` | `<Link to="/tablero">` |
| `src/components/not-found.tsx` | 12 | `<Link to="/">` | `<Link to="/tablero">` |
| `src/features/cuenta/crear-libro.tsx` | 53 | `window.location.assign('/')` | `window.location.assign('/tablero')` |
| `src/features/cuenta/tus-libros.tsx` | 56 | `window.location.assign('/')` | `window.location.assign('/tablero')` |
| `src/routes/entrar.tsx` | 33 | `to: redirigirA ?? '/'` | `to: redirigirA ?? '/tablero'` |
| `src/routes/crear-cuenta.tsx` | 50 y 64 | `to: '/'` | `to: '/tablero'` |
| `src/routes/guia.tsx` | 71 | `to: '/'` | `to: '/tablero'` |
| `src/routes/contabilidad.patrimonio.tsx` | 66 | `<Link to="/">` | `<Link to="/tablero">` |

**`src/components/nav-user.tsx:119` no se toca.** Ese `window.location.assign('/')` es el de cerrar sesión, y con la landing en la raíz pasa a hacer exactamente lo que corresponde: quien sale cae en la cara pública del producto en vez de en un formulario de ingreso. Los `assign('/')` que sí cambian son los de crear y de cambiar de libro, que recargan la app entera para vaciar la memoria y tienen que volver al tablero.

- [ ] **Step 8: Comprobar que no quedó ninguna**

Run: `grep -rn "to=\"/\"\|to: '/'\|assign('/')" src --include=*.ts --include=*.tsx | grep -v routeTree`
Expected: una sola línea, `src/components/nav-user.tsx:119`, que es la de cerrar sesión y se queda así a propósito.

- [ ] **Step 9: Correr todo**

```bash
npx tsc --noEmit -p tsconfig.app.json
npx vitest run
npm run build
```

Expected: tipos limpios, las pruebas en verde —las 46 de antes más las que agregaste— y el build sin errores.

- [ ] **Step 10: Commitear**

```bash
npx prettier --no-semi --single-quote --print-width 100 --write src/routes src/features/shell src/features/shortcuts src/components src/features/cuenta
git add -A
git commit -m "✨ feat: la landing es la raíz y el tablero se muda a /tablero"
```

---

### Task 6: Que la T se escriba sola

El gesto que demuestra el titular: sin él, el titular hay que creerlo.

**Files:**
- Modify: `web/src/features/landing/cuenta-t.tsx`
- Modify: `web/src/styles.css` (un `@keyframes` al lado de los que ya hay, cerca de la línea 281)

- [ ] **Step 1: Agregar la animación al CSS**

En `web/src/styles.css`, junto a los otros `@keyframes` del archivo:

```css
  /* La landing: cada anotación aparece un suspiro después de la frase que la explica. Sube
     cuatro píxeles porque una cifra que solo se desvanece no parece escribirse, parece
     prenderse. */
  @keyframes asiento-entra {
    from {
      opacity: 0;
      transform: translateY(4px);
    }
    to {
      opacity: 1;
      transform: none;
    }
  }

  .asiento-entra {
    animation: asiento-entra 420ms cubic-bezier(0.22, 1, 0.36, 1) backwards;
  }

  /* Quien pidió menos movimiento ve la pantalla entera de una. No es una degradación: la
     página dice lo mismo sin escribirse sola. */
  @media (prefers-reduced-motion: reduce) {
    .asiento-entra {
      animation: none;
    }
  }
```

- [ ] **Step 2: Escalonar los asientos**

En `cuenta-t.tsx`, dentro del `.map`, agregar el retraso por índice. Cambiar la firma del map a `(asiento, indice)` y aplicar a los tres elementos del par:

```tsx
        {ASIENTOS.map((asiento, indice) => (
          <div key={asiento.glosa} className="contents">
            <p
              className="asiento-entra col-span-full pt-6 pb-1.5 text-center text-sm text-muted-foreground"
              style={{ animationDelay: `${String(indice * 260)}ms` }}
            >
              «{asiento.glosa}»
            </p>

            <p
              className="asiento-entra border-l border-border pl-3 text-sm md:border-0 md:pr-8 md:pl-0"
              style={{ animationDelay: `${String(indice * 260 + 140)}ms` }}
            >
              <Linea {...asiento.debe} />
            </p>
            <p
              className="asiento-entra border-l border-border pl-3 text-sm md:border-0 md:pl-8"
              style={{ animationDelay: `${String(indice * 260 + 220)}ms` }}
            >
              <Linea {...asiento.haber} />
            </p>
          </div>
        ))}
```

`backwards` en la animación es lo que mantiene invisible lo que todavía no entró: sin eso, los tres pares se ven desde el primer cuadro y después parpadean.

El total: 3 × 260 + 220 + 420 ≈ 1,4 s hasta el último renglón. Si al verlo se siente lento, bajá el 260 antes de tocar cualquier otra cosa.

- [ ] **Step 3: Encender el par que se está mirando**

En el `<div className="contents">` de cada asiento no se puede poner `group`, porque `display: contents` no pinta nada. El encendido va en el contenedor de la grilla, con `has`:

```tsx
      <div className="grid grid-cols-1 gap-x-0 md:grid-cols-2 [&:hover>p]:text-muted-foreground [&>p:hover]:text-foreground">
```

Si al probarlo el efecto se siente confuso —los tres renglones de un par no se encienden juntos, porque son tres `<p>` hermanos y no un bloque—, **sacalo**. Es un realce, no un requisito: la página se lee igual sin él, y medio efecto es peor que ninguno.

- [ ] **Step 4: Verlo**

```bash
npm run dev
```

Abrí `http://localhost:5173/`, recargá y mirá que la T se escriba de arriba abajo. Después:

- Con `prefers-reduced-motion` activado en el sistema (o en las DevTools: Rendering → Emulate CSS media feature prefers-reduced-motion), recargá: todo tiene que aparecer de una, sin movimiento.
- A 320 píxeles de ancho: la T se apila, no hay scroll horizontal y no se corta ninguna cifra.
- Que se vea en papel blanco aunque el sistema esté en oscuro, y que al volver a
  cualquier pantalla con sesión el tema guardado vuelva solo.

- [ ] **Step 5: Commitear**

```bash
npx prettier --no-semi --single-quote --print-width 100 --write src/features/landing src/styles.css
git add src/features/landing src/styles.css
git commit -m "✨ feat: la cuenta T se escribe sola al cargar la landing"
```

---

### Task 7: Cierre

- [ ] **Step 1: Correr todo de nuevo**

```bash
npx vitest run
npm run build
cd ../api && npm run lint
```

Expected: verde, verde y verde.

- [ ] **Step 2: Comprobar los caminos que la muda pudo romper**

Con `npm run dev` andando, a mano:

1. Sin sesión, entrar a `/` → se ve la landing.
2. Sin sesión, entrar a `/contabilidad/movimientos` → manda a `/entrar` y, después de entrar, vuelve a movimientos.
3. Con sesión, entrar a `/` → salta a `/tablero`.
4. Entrar desde la landing → cae en `/tablero`.
5. El atajo `g h` → `/tablero`.
6. El logo de la barra lateral → `/tablero`.
7. Una URL inventada → «Esta dirección no existe», y su botón lleva a `/tablero`.
8. Crear un libro → recarga y cae en `/tablero`.

- [ ] **Step 3: Commitear lo que haya quedado suelto**

```bash
git status --porcelain
```

Si hay algo, commitealo con su mensaje. Si no hay nada, la landing está lista.
